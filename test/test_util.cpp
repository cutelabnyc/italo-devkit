#include "test_util.hpp"
#include <stdio.h>
void run_equality_test(void *handle, processor_t process, uint16_t *ins,
                       uint16_t *outs, uint16_t *expected) {
  /*for (uint16_t i = 0; i < count; i++) {
    process(handle, ins + i, outs + i, random_reset + i, density_input + i,
            autopulse_output + i, msec);
  }*/
  // for (uint16_t i = 0; i < count; i++) {
  // TEST_ASSERT_EQUAL_UINT16(expected[i], outs[i]);
  // }
  printf("Hello Worldy\n");
}
